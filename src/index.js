import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // This will import global CSS, if you have it
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
